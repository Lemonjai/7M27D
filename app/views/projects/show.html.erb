<%= @project.title %>
<br>
<%= @project.content %>
<br>
<%= @project.img %>

<br>
<br>

<%= link_to "Edit", edit_project_path(@project) %>
<%= link_to "Delete", project_path(@project), method: :delete, data: {confirm: "Are you sure you wanted to delete this post?"} %>